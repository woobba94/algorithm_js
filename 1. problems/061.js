// 문자열을 입력받고 연속되는 문자열을 압축해서 표현

let testCase = ['aaabbbbcdddd', 'abcdddd', 'abcabcabcab', 'bbbbbbbbbbbbbbbb', 'aabbaabbaabb'];

for (let k = 0; k < testCase.length; k++) {
  let input = testCase[k];
}

function getPressStr(input) {
  let result = '';

  return result;
}
